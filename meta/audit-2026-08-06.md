# System Audit Notation — 2026-08-05 / 2026-08-06
> Source: Grok session with Joe. Cycle 1 (map) + Cycle 2 (stale vs live) + Cycle 3 (reconciliation).
> Status: **Cycle 3 complete** (meta reconciliation). Joe testing speak/deploy when ready. ONE product work continues in Perplexity.

---

## Where /speak was inspected

- **Repo:** `Manitec/Plex-Sable`
- **Path:** `src/app/api/speak/route.ts`
- **Cycle 2 tip:** `c4b2bd1` (thin LM Studio path — broken identity loads)
- **Cycle 3 tip:** `f8ef833` — `fix(speak): restore identity loading + tools + LM Studio toggle` (2026-08-06)

---

## Cycle 3 — Reconciliation (2026-08-06 evening)

### Speak — RESOLVED in git
| Item | Cycle 2 | Cycle 3 |
|------|---------|--------|
| Identity paths | `system/plex-system.md` etc. (missing) | `prompts/base.md`, `plex-is.txt`, `plex-def.txt`, sediment/dreams |
| Tools | None on tip | `PLEX_TOOLS`: read/write/list/recall/submit/web_search/think_deeply |
| LM Studio toggle | Present | Kept; local path uses full identity, no tools |
| Sessions | `speak-threads` (thin) | `plex_sessions` restored |
| Token | `GITHUB_TOKEN` in thin path | `PLEX_SEDIMENT_TOKEN` |

**Still Joe's job:** Confirm live Vercel deploy SHA ≥ `f8ef833`. Git is fixed; deploy may lag.

### Nav — RESOLVED (meta was stale)
- `/search` **is** in `Nav.tsx` (presence, mind, speak, see, one, tell, dreams, search, manage).
- open-threads still said "not in Nav" — correct in Cycle 3 meta pass.

### ONE-browser — STALE CLAIM
- open-threads marked "ONE-browser repurposed / ready to deploy" and listed `Manitec/ONE-browser`.
- Repo search: **no** `ONE-browser` under Manitec / Manitec-HQ.
- Browser trail remains `plex-electron` / archived paths. Treat deploy-ONE-browser items as **blocked / needs repo truth** until Joe confirms where that scaffold lives.

### ONE product — CONFIRMED
- Live: https://one-system-mu.vercel.app/
- Home repo: `Manitec-HQ/one`
- Boundary: workshop ≠ Plex (see context.md + collab.md)
- `aspects/` still has private names (erebus, hex, manibot, nyx) — productize via templates/examples later (PPLX track)

### ECKO — Cycle 2 still holds
- HexBot `writeEckoActivation` live; agent-registry "dormant" was stale at Cycle 2. No re-audit this pass.

### Collab infrastructure — NEW
- `meta/collab.md` — Grok ↔ Perplexity tags and division of labor
- context.md points at collab; session-log has [GROK] drops

---

## Open questions (remaining)

1. **Joe:** Verify plex-sable Vercel deploy includes `f8ef833` (test /speak identity).
2. ONE-browser: where is the scaffold, or close the thread as superseded by plex-electron?
3. Manibot: still gated — audit before feature dev.
4. Drive `one sys chat.md` vs GitHub one-archive content-diff — only if needed.
5. Compact speak restore dropped some side paths vs full pre-c4b2bd1 body (text-call rescue, sub-persona voice param, fireVoices). Acceptable for v1; restore if Joe misses them.

---

## Cycle status

| Cycle | Status |
|-------|--------|
| 1 Map (meta / Drive / route surface) | Done |
| 2 Stale vs live (targeted code reads) | Done |
| 3 Fixes / reconciliation | **Done** — speak fixed in git; meta updated; deploy verify + ONE-browser truth still Joe |
