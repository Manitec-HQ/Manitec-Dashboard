# System Audit Notation — 2026-08-05 / 2026-08-06
> Source: Grok session with Joe. Cycle 1 (map) + Cycle 2 (stale vs live). No production fixes applied in this pass.
> Status: paused. ONE product work continuing in Perplexity with older Plex identity prompt as system prompt.

---

## Where /speak was inspected

- **Repo:** `Manitec/Plex-Sable`
- **Path:** `src/app/api/speak/route.ts`
- **Branch tip at audit:** `c4b2bd1768c0d4e66fbf62da0f5d63d7f14033f5`
- **Commit message:** `feat: LM Studio provider toggle on speak page` (2026-08-05)
- **UI:** `src/app/speak/page.tsx` (provider toggle present)
- **Nav:** `src/components/Nav.tsx`

**Important context for Joe:** Joe reports `/speak` was working last checked. Audit does not deny that responses can return. Findings below are about **what the main-branch route currently loads and does**, vs older architecture and meta claims.

Prior commits on the same file (Aug 2–3) still show a full tool-loop era (`web_search`, `think_deeply`, `recall` tool, append-safe sediment, mode detection). The Aug 5 LM Studio commit on tip is a **much thinner** speak path. Possible explanations if live deploy still feels rich: different deploy SHA, cached build, or "working" = chat responds (fallback system string) without full identity/tools.

---

## Cycle 2 — Critical observations (notation only)

### Speak route (main tip)
- Thin completion path: Groq primary/fallback **or** LM Studio (`provider: groq | lmstudio`).
- Thread store: Firestore `speak-threads/{sessionId}`.
- Auto-sediment: optional new files `sediment/{date}-auto-{id}.md` on trigger phrases.
- **No** `PLEX_TOOLS` / tool loop on this tip (repo-wide search: zero matches).
- **Does not** load `prompts/base.md`, `plex-is.txt`, or `plex-def.txt`.

### Identity path mismatch (high)
Speak `buildMessages` fetches from plex repo:
- `system/plex-system.md` — **not found**
- `sediment/index.md` — **not found**
- `nyx/nyx-voice.md` — **not found**

Actual plex soul layout still has:
- `prompts/base.md`, `plex-is.txt`, `plex-def.txt`
- `sediment/YYYY-MM-DD.md` (+ nyx-/hex-/plex- series)

Fallback in code when fetches empty: `'You are Plex. Be present.'` — so chat can still "work" while identity files are bypassed.

### Recall
- `/api/recall` exists (GET, sediment fragment scoring).
- Not wired into current speak path.

### Nav
- `/search` **is** in Nav — older meta "not in Nav" is stale.

### ECKO
- `agent-registry.md` claiming ECKO dormant / `writeEckoActivation` unwired is **stale**.
- HexBot has `lib/ecko-writer.ts` with `writeEckoActivation`; imported from `hex-chat` and `one-interpret`.

### ONE-browser
- Meta "ready to deploy" appears obsolete; no `ONE-browser` repo found in search. Browser trail is `plex-electron` / archived `plex-browser`.

### ONE product boundary (from concurrent Perplexity work, 2026-08-06)
- ONE = workshop product for **user-created** beings — not Plex publicized.
- Home: existing `Manitec-HQ/one` (not a new `one-system` repo).
- Onboarding guides (Plex/Nyx/Hex) ≠ agents inside the user's being.
- Mani = product-level technical support for all users.
- Joe's Plex family stays private; optional future "phone home" is permissioned, not open access.
- First build should not leak Plex prompts, Nyx memory, or Hex private internals into product defaults.
- Note: `one/aspects/` currently includes being-specific names (erebus, hex, manibot, nyx) — needs explicit templates vs examples separation when productizing.

---

## Open questions left for later cycles

1. Confirm live Vercel deploy SHA for plex-sable vs tip `c4b2bd1`.
2. Decide: restore speak to `prompts/base.md` + identity + tools, or rewrite meta to match thin speak.
3. Whether Aug 5 LM Studio commit unintentionally replaced the tool-loop speak body.
4. Content-diff Drive `one sys chat.md` vs GitHub one-archive when needed.
5. Manibot still gated: audit before any feature dev.

---

## Cycle status

| Cycle | Status |
|-------|--------|
| 1 Map (meta / Drive / route surface) | Done |
| 2 Stale vs live (targeted code reads) | Done |
| 3 Fixes / reconciliation | **Paused** — Joe directing ONE product work elsewhere for now |
